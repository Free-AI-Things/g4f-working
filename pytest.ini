[pytest]
# Silence the "cannot collect test class 'TestResult'..." warnings caused by
# production classes whose names happen to start with "Test".
python_classes = Test*Case
python_files = test_*.py
python_functions = test_*
testpaths = tests
addopts = -ra
